<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Daily Briefing</title>
  <style>
    /* Reset & Core */
    *, *::before, *::after {
      box-sizing: border-box; /* Global fix for sizing */
    }

    :root {
      --bg-body: #f4f4f7;
      --bg-sidebar: #1e1e24;
      --bg-card: #ffffff;
      --text-main: #2c2c35;
      --text-muted: #6e6e78;
      --text-light: #a0a0a8;
      --text-sidebar: #e0e0e0;
      --accent: #2563eb;
      --accent-soft: #eff4ff;
      --border-light: #e6e6eb;
      --shadow-sm: 0 1px 3px rgba(0,0,0,0.05);
      --shadow-md: 0 4px 12px rgba(0,0,0,0.08);
      --font-sans: -apple-system, BlinkMacSystemFont, "Inter", "Segoe UI", Roboto, Helvetica, Arial, sans-serif;
      --font-serif: "Merriweather", "Georgia", serif;
      
      /* Status Colors */
      --c-success: #059669;
      --c-bg-success: #ecfdf5;
      --c-warning: #d97706;
      --c-bg-warning: #fffbeb;
      --c-danger: #dc2626;
      --c-bg-danger: #fef2f2;
    }

    body {
      margin: 0;
      padding: 0;
      background-color: var(--bg-body);
      font-family: var(--font-sans);
      color: var(--text-main);
      -webkit-font-smoothing: antialiased;
    }

    /* Layout: Sidebar + Main */
    .layout-wrapper {
      max-width: 1200px; /* Increased width for better PC view */
      margin: 0 auto;
      display: flex;
      align-items: flex-start; /* Important for sticky sidebar */
      position: relative;
    }

    /* Sidebar - Sticky Fix */
    .sidebar {
      width: 260px;
      background-color: var(--bg-sidebar);
      color: var(--text-sidebar);
      padding: 32px 24px;
      
      /* Sticky Logic */
      position: -webkit-sticky;
      position: sticky;
      top: 0;
      height: 100vh;
      overflow-y: auto;
      
      flex-shrink: 0;
      display: flex;
      flex-direction: column;
    }

    .brand {
      font-family: var(--font-serif);
      font-size: 22px;
      font-weight: 700;
      color: #fff;
      margin-bottom: 4px;
      letter-spacing: -0.5px;
    }

    .date-sub {
      font-size: 13px;
      color: #888;
      margin-bottom: 32px;
      display: block;
      padding-bottom: 12px;
      border-bottom: 1px solid rgba(255,255,255,0.1);
    }

    .nav-menu {
      list-style: none;
      padding: 0;
      margin: 0 0 auto 0; /* Pushes content below to bottom if needed */
    }

    .nav-item {
      margin-bottom: 4px;
    }

    .nav-link {
      display: flex;
      justify-content: space-between;
      align-items: center;
      color: #aaa;
      text-decoration: none;
      font-size: 14px;
      padding: 10px 12px;
      border-radius: 8px;
      transition: all 0.2s;
    }
    
    .nav-link:hover {
      background-color: rgba(255,255,255,0.1);
      color: #fff;
    }
    
    .nav-link.active {
      background-color: var(--accent);
      color: #fff;
      font-weight: 500;
    }
    
    .nav-badge {
      font-size: 11px;
      font-weight: 600;
      background: rgba(255,255,255,0.15);
      color: #fff;
      padding: 2px 8px;
      border-radius: 12px;
    }

    .stats-box {
      background: rgba(0,0,0,0.2);
      border-radius: 8px;
      padding: 16px;
      font-size: 12px;
      color: #aaa;
      margin-top: 32px;
    }
    
    .stat-row {
      display: flex;
      justify-content: space-between;
      margin-bottom: 8px;
    }
    .stat-row:last-child { margin-bottom: 0; }
    .stat-val {
      color: #fff;
      font-weight: 600;
      font-family: var(--font-sans);
      font-variant-numeric: tabular-nums;
      letter-spacing: 0.2px;
      text-align: right;
      min-width: 120px;
    }

    /* Main Content */
    .main-content {
      flex: 1;
      padding: 40px;
      min-width: 0; /* Fix flex overflow */
    }

    /* Typography & Headers */
    h2.section-heading {
      font-family: var(--font-serif);
      font-size: 24px;
      color: var(--text-main);
      margin: 0 0 24px 0;
      padding-bottom: 12px;
      border-bottom: 1px solid var(--border-light);
      display: flex;
      align-items: center;
      gap: 12px;
      scroll-margin-top: 20px; /* Space when jumping to anchor */
    }
    
    section { margin-bottom: 60px; }

    /* Cards */
    .card {
      background: var(--bg-card);
      border-radius: 12px;
      padding: 24px;
      margin-bottom: 24px;
      box-shadow: var(--shadow-sm);
      border: 1px solid var(--border-light);
      transition: box-shadow 0.2s, transform 0.2s;
    }
    .card:hover { 
      box-shadow: var(--shadow-md); 
      transform: translateY(-2px);
    }

    .card.featured {
      border-left: 4px solid var(--accent);
    }
    
    .card-header {
      display: flex;
      justify-content: space-between;
      align-items: flex-start;
      margin-bottom: 12px;
      gap: 16px;
    }
    .card-header-right {
      display: flex;
      flex-direction: column;
      align-items: flex-end;
      gap: 6px;
      flex-shrink: 0;
    }
    .header-badges {
      display: flex;
      gap: 6px;
      flex-wrap: wrap;
      justify-content: flex-end;
    }
    
    .card-title {
      font-size: 18px;
      font-weight: 700;
      line-height: 1.4;
      margin: 0;
      color: var(--text-main);
    }
    
    .card-title a { text-decoration: none; color: inherit; }
    .card-title a:hover { color: var(--accent); }

    .score-badge {
      background: #fff;
      color: var(--text-main);
      font-weight: 700;
      font-size: 13px;
      padding: 4px 10px;
      border-radius: 6px;
      border: 1px solid var(--border-light);
      white-space: nowrap;
    }

    .meta-row {
      font-size: 13px;
      color: var(--text-muted);
      display: flex;
      flex-wrap: wrap;
      gap: 12px;
      margin-bottom: 16px;
      align-items: center;
    }
    
    .meta-item { display: flex; align-items: center; gap: 4px; }
    
    /* Summary Box */
    .summary-box {
      background: var(--bg-body);
      border-radius: 8px;
      padding: 16px;
      margin-bottom: 16px;
    }
    
    .summary-list {
      margin: 0;
      padding-left: 18px;
      font-size: 14px;
      line-height: 1.6;
      color: #374151;
    }
    .summary-list li { margin-bottom: 6px; }

    /* Analysis Tags */
    .analysis-section {
      margin-top: 16px;
      padding-top: 16px;
      border-top: 1px dashed var(--border-light);
    }

    .score-legend {
      font-size: 11px;
      color: #6b7280;
      margin-bottom: 8px;
      line-height: 1.4;
    }
    
    .reason-item {
      font-size: 13px;
      color: var(--text-muted);
      margin-bottom: 6px;
      display: grid;
      grid-template-columns: 140px 1fr;
      column-gap: 8px;
      row-gap: 2px;
      align-items: start;
    }
    
    .tag {
      font-size: 10px;
      text-transform: uppercase;
      font-weight: 700;
      letter-spacing: 0.5px;
      padding: 2px 6px;
      border-radius: 4px;
      background: #e5e7eb;
      color: #4b5563;
      white-space: nowrap;
      flex-shrink: 0;
      text-align: right;
    }
    
    .tag.funding { background: var(--c-bg-success); color: var(--c-success); }
    .tag.urgent { background: var(--c-bg-danger); color: var(--c-danger); }
    .tag.topic { background: #dbeafe; color: #1e40af; }
    .tag.contract { background: #eff6ff; color: #1e40af; }
    .tag.score { background: #f1f5f9; color: #0f172a; }
    .tag.value { background: #ecfdf3; color: #047857; }
    .tag.limit { background: #fff7ed; color: #9a3412; }

    /* Compact Grid (Masonry-ish) */
    .compact-grid {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
      gap: 20px;
    }

    .mini-card {
      background: var(--bg-card);
      padding: 16px;
      border-radius: 8px;
      border: 1px solid var(--border-light);
      box-shadow: var(--shadow-sm);
      display: flex;
      flex-direction: column;
      height: 100%;
      transition: border-color 0.2s;
    }
    .mini-card:hover { border-color: #cbd5e1; }
    
    .mini-title {
      font-size: 15px;
      font-weight: 600;
      margin: 0 0 8px 0;
      line-height: 1.4;
    }
    .mini-title a { text-decoration: none; color: var(--text-main); }
    .mini-title a:hover { color: var(--accent); }
    
    .mini-body { flex: 1; }

    .mini-summary {
      font-size: 12px;
      color: #888;
      line-height: 1.4;
      display: -webkit-box;
      -webkit-line-clamp: 2;
      -webkit-box-orient: vertical;
      overflow: hidden;
      margin-bottom: 8px;
    }

    .mini-badges {
      display: flex;
      flex-wrap: wrap;
      gap: 6px;
      margin-bottom: 6px;
    }
    
    .mini-meta {
      font-size: 12px;
      color: var(--text-light);
      display: flex;
      justify-content: space-between;
      align-items: flex-end;
      margin-top: 12px;
      padding-top: 12px;
      border-top: 1px solid #f0f0f4;
    }

    /* Responsive / Mobile */
    @media (max-width: 850px) {
      .layout-wrapper { flex-direction: column; }
      
      .sidebar {
        width: 100%;
        height: auto;
        position: relative; /* Disable sticky on mobile */
        top: auto;
        padding: 20px;
        overflow-y: visible;
      }
      
      .nav-menu { 
        display: flex; 
        flex-wrap: wrap; 
        gap: 8px; 
        margin-bottom: 20px;
      }
      
      .nav-item { margin: 0; }
      .nav-link { background: rgba(255,255,255,0.05); padding: 6px 12px; }
      
      .stats-box { display: none; } /* Hide stats on mobile to save space */
      
      .main-content { padding: 24px 16px; }
      .compact-grid { grid-template-columns: 1fr; }
    }
  </style>
</head>
<body>

  {% macro render_badge(label, type='default') -%}
    {% set class = 'tag ' + type %}
    <span class="{{ class }}">{{ label }}</span>
  {%- endmacro %}

  {% macro render_reason(r) -%}
    {% if r and r.startswith("[") and "]" in r %}
      {% set parts = r.split("]", 1) %}
      {% set label = parts[0][1:] %}
      {% set content = parts[1].strip() %}
      {% set type = 'default' %}
      {% if 'Funding' in label or '资金' in label %} {% set type = 'funding' %} {% endif %}
      {% if 'Topic' in label or '主题' in label %} {% set type = 'topic' %} {% endif %}
      {% if 'Requirement' in label or '要求' in label %} {% set type = 'urgent' %} {% endif %}
      {% if 'Score breakdown' in label or '评分' in label %} {% set type = 'score' %} {% endif %}
      {% if 'Critical value' in label or '关键价值' in label or '核心价值' in label %} {% set type = 'value' %} {% endif %}
      {% if 'Limitations' in label or '局限' in label or '限制' in label %} {% set type = 'limit' %} {% endif %}
      <div class="reason-item">
        {{ render_badge(label, type) }}
        <span>{{ content }}</span>
      </div>
    {% else %}
      <div class="reason-item">
        <span style="color: var(--accent);">•</span>
        <span>{{ r }}</span>
      </div>
    {% endif %}
  {%- endmacro %}

  <div class="layout-wrapper">
    
    <!-- Left Sidebar -->
    <aside class="sidebar">
      <div>
        <div class="brand">foryourseek</div>
        <span class="date-sub">{{ date }}</span>
        
        <ul class="nav-menu">
          <li class="nav-item">
            <a href="#papers-strong" class="nav-link active">
              Strong Papers <span class="nav-badge">{{ papers_strong|length }}</span>
            </a>
          </li>
          <li class="nav-item">
            <a href="#jobs-top" class="nav-link">
              Top Jobs <span class="nav-badge">{{ jobs_top|length }}</span>
            </a>
          </li>
          <li class="nav-item">
            <a href="#jobs-list" class="nav-link">
              All Jobs <span class="nav-badge">{{ jobs_list|length }}</span>
            </a>
          </li>
          <li class="nav-item">
            <a href="#papers-list" class="nav-link">
              More Papers <span class="nav-badge">{{ papers_normal|length }}</span>
            </a>
          </li>
        </ul>
      </div>

      <div class="stats-box">
        <div class="stat-row"><span>Run ID</span> <span class="stat-val">{{ run_id }}</span></div>
        <div class="stat-row"><span>Model</span> <span class="stat-val">{{ llm_model }}</span></div>
        <div class="stat-row"><span>Calls</span> <span class="stat-val">{{ llm_calls_display }}</span></div>
        <div class="stat-row"><span>Tokens</span> <span class="stat-val">{{ llm_tokens_display }}</span></div>
        <div class="stat-row"><span>Cost</span> <span class="stat-val">${{ "%.4f"|format(llm_cost_usd) }}</span></div>
        <div class="stat-row"><span>Errors</span> <span class="stat-val" style="color: {{ 'var(--c-danger)' if error_count > 0 else 'inherit' }}">{{ error_count }}</span></div>
        <div class="stat-row"><span>Time</span> <span class="stat-val">{{ duration_str }}</span></div>
      </div>
    </aside>

    <!-- Main Content -->
    <main class="main-content">
      
      <!-- 1. Strong Papers (Full Cards) -->
      {% if papers_strong %}
      <section id="papers-strong">
        <h2 class="section-heading">📌 Must Read Papers</h2>
        {% for p in papers_strong %}
        <article class="card featured">
          <div class="card-header">
            <h3 class="card-title"><a href="{{ p.url }}" target="_blank">{{ p.title }}</a></h3>
            <span class="score-badge" style="color: var(--c-success); border-color: var(--c-success);">{{ p.relevance_score }}</span>
          </div>
          <div class="meta-row">
            <div class="meta-item"><span>📖 {{ p.journal_norm or p.journal }}</span></div>
            <div class="meta-item"><span>📅 {{ p.published_at_norm or p.published_at }}</span></div>
            {% if p.doi %}
            <div class="meta-item"><span>🔗 DOI: {{ p.doi }}</span></div>
            {% endif %}
          </div>
          
          {% if p.summary_bullets %}
          <div class="summary-box">
            <ul class="summary-list">
              {% for b in p.summary_bullets %}
              <li>{{ b }}</li>
              {% endfor %}
            </ul>
          </div>
          {% endif %}
          
          {% if p.reasons %}
          <div class="analysis-section">
            {% set ns = namespace(has_score=false) %}
            {% for r in p.reasons %}
              {% if '[Score breakdown]' in r or '[评分' in r %}
                {% set ns.has_score = true %}
              {% endif %}
            {% endfor %}
            {% if ns.has_score %}
            {% if output_language == "en" %}
            <div class="score-legend">A=Topic Match (0–3), B=Technical Depth (0–3), C=Utility &amp; Novelty (0–2), D=Evidence Strength (0–2)</div>
            {% else %}
            <div class="score-legend">A=主题匹配 (0–3), B=技术深度 (0–3), C=实用性/新颖性 (0–2), D=证据强度 (0–2)</div>
            {% endif %}
            {% endif %}
            {% for r in p.reasons %}
              {{ render_reason(r) }}
            {% endfor %}
          </div>
          {% endif %}
          
          <div style="margin-top: 12px; font-size: 10px; color: #ccc;">
            Source: {{ p.abstract_source }} | Method: {{ p.extract_method }}
          </div>
        </article>
        {% endfor %}
      </section>
      {% endif %}

      <!-- 2. Top Jobs (Full Cards) -->
      {% if jobs_top %}
      <section id="jobs-top">
        <h2 class="section-heading">🏆 Top PhD Opportunities</h2>
        <div class="compact-grid" style="grid-template-columns: 1fr;"> <!-- Force full width for top jobs -->
        {% for j in jobs_top %}
        <article class="card">
          <div class="card-header">
            <h3 class="card-title"><a href="{{ j.url }}" target="_blank">{{ j.title }}</a></h3>
            <div class="card-header-right">
              <span class="score-badge">{{ j.relevance_score }}</span>
              {% if j.salary or j.contract_type or j.funding_programme %}
              <div class="header-badges">
                {% if j.salary %}{{ render_badge('💰 ' + (j.salary_norm or j.salary), 'funding') }}{% endif %}
                {% if j.contract_type %}{{ render_badge('📝 ' + (j.contract_type_norm or j.contract_type)) }}{% endif %}
                {% if j.funding_programme %}{{ render_badge('⭐ ' + j.funding_programme, 'topic') }}{% endif %}
              </div>
              {% endif %}
            </div>
          </div>
          <div class="meta-row">
            <div class="meta-item"><span>🏛️ {{ j.org_norm or j.org }}</span></div>
            <div class="meta-item"><span>📍 {{ j.location_norm or j.location }}</span></div>
            {% if j.deadline %}<div class="meta-item"><span style="color: var(--c-danger);">⏰ {{ j.deadline_norm or j.deadline }}</span></div>{% endif %}
          </div>

          {% if j.summary_bullets %}
          <div class="summary-box" style="background: #fdfdfd; border: 1px solid #eee;">
            <ul class="summary-list">
              {% for b in j.summary_bullets %}
              <li>{{ b }}</li>
              {% endfor %}
            </ul>
          </div>
          {% endif %}

          {% if j.reasons %}
          <div class="analysis-section">
            {% for r in j.reasons %}
              {{ render_reason(r) }}
            {% endfor %}
          </div>
          {% endif %}
        </article>
        {% endfor %}
        </div>
      </section>
      {% endif %}

      <!-- 3. Other Jobs (Compact Grid) -->
      {% if jobs_list %}
      <section id="jobs-list">
        <h2 class="section-heading">💼 More PhD Positions</h2>
        <div class="compact-grid">
          {% for j in jobs_list %}
          <div class="mini-card">
            <h4 class="mini-title"><a href="{{ j.url }}" target="_blank">{{ j.title }}</a></h4>
            <div style="font-size: 13px; color: var(--text-muted); margin-bottom: 8px;">
              {{ j.org_norm or j.org }} • {{ j.location_norm or j.location }}
            </div>
            {% if j.salary or j.contract_type or j.funding_programme %}
            <div class="mini-badges">
              {% if j.salary %}{{ render_badge('💰 ' + (j.salary_norm or j.salary), 'funding') }}{% endif %}
              {% if j.contract_type %}{{ render_badge('📝 ' + (j.contract_type_norm or j.contract_type)) }}{% endif %}
              {% if j.funding_programme %}{{ render_badge('⭐ ' + j.funding_programme, 'topic') }}{% endif %}
            </div>
            {% endif %}
            {% if j.summary_bullets %}
            <div class="mini-summary">{{ j.summary_bullets[0] }}</div>
            {% endif %}
            <div class="mini-meta">
               <span style="font-weight: 600; color: {{ 'var(--c-warning)' if j.relevance_score > 5 else 'var(--text-light)' }}">{{ j.relevance_score }}/10</span>
               {% if j.deadline %}<span>⏰ {{ j.deadline_norm or j.deadline }}</span>{% endif %}
            </div>
          </div>
          {% endfor %}
        </div>
      </section>
      {% endif %}

      <!-- 4. Other Papers (Compact Grid) -->
      {% if papers_normal %}
      <section id="papers-list">
        <h2 class="section-heading">📄 More Papers</h2>
        <div class="compact-grid">
          {% for p in papers_normal %}
          <div class="mini-card">
            <h4 class="mini-title"><a href="{{ p.url }}" target="_blank">{{ p.title }}</a></h4>
            <div style="font-size: 12px; color: var(--text-muted); margin-bottom: 6px;">
              {{ p.journal_norm or p.journal }}
            </div>
            <div class="mini-meta">
               <span style="font-weight: 600; color: {{ 'var(--c-warning)' if p.relevance_score > 5 else 'var(--text-light)' }}">{{ p.relevance_score }}/10</span>
               <span>📅 {{ p.published_at_norm or p.published_at }}</span>
            </div>
            {% if p.abstract_preview %}
            <div style="font-size: 12px; color: #888; display: -webkit-box; -webkit-line-clamp: 2; -webkit-box-orient: vertical; overflow: hidden;">
              {{ p.abstract_preview }}
            </div>
            {% endif %}
          </div>
          {% endfor %}
        </div>
      </section>
      {% endif %}

      <!-- Random Review -->
      {% if random_review %}
      <section style="margin-top: 40px; border-top: 1px dashed #ccc; padding-top: 20px;">
        <h4 style="color: #999; font-size: 14px; text-transform: uppercase; margin-bottom: 16px;">🔁 Flashback Review</h4>
        <article class="card" style="opacity: 0.8;">
          <h3 class="card-title"><a href="{{ random_review.url }}" target="_blank">{{ random_review.title }}</a></h3>
          <div class="meta-row" style="margin-top: 8px;">
            <span>{{ random_review.journal_norm or random_review.journal }}</span>
            <span>{{ random_review.published_at_norm or random_review.published_at }}</span>
          </div>
          <ul class="summary-list" style="margin-top: 12px; color: #666;">
            {% for b in random_review.summary_bullets %}<li>{{ b }}</li>{% endfor %}
          </ul>
        </article>
      </section>
      {% endif %}
      
      <div style="margin-top: 60px; text-align: center; color: #ccc; font-size: 11px;">
        foryourseek AI Agent • {{ date }} • {{ run_id }}
      </div>

    </main>
  </div>
</body>
</html>
